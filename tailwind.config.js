/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./docs/*.html'],
    theme: {
      extend: {
        // You can add customizations here, for example, custom colors, fonts, etc.
      },
    },
    plugins: [],
  }
  